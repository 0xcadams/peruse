name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  test:
    name: Test (${{ matrix.os }})
    permissions:
      contents: read # Required to check out the repository.
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.14"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check
        run: bun run check

      - name: Test
        run: bun run test

      - name: Build standalone executable
        run: bun run binary

      - name: Smoke standalone executable
        shell: bash
        env:
          BINARY_SUFFIX: ${{ runner.os == 'Windows' && '.exe' || '' }}
        run: |
          ./dist/peruse${BINARY_SUFFIX} --port 18080 demo-files &
          pid=$!
          trap 'kill "${pid}" >/dev/null 2>&1 || true' EXIT
          curl --fail --retry 10 --retry-all-errors --retry-delay 1 http://127.0.0.1:18080/__peruse/health

  container:
    name: Container
    permissions:
      contents: read # Required to check out the repository.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build image
        run: docker build --build-arg VERSION=ci -t peruse:ci .

      - name: Smoke read-only image
        shell: bash
        run: |
          docker run --rm \
            --read-only \
            --cap-drop=ALL \
            --security-opt=no-new-privileges \
            --mount type=bind,src="${GITHUB_WORKSPACE}",dst=/data,readonly \
            --entrypoint /usr/bin/git \
            peruse:ci -C /data status --short

          docker run --rm -d \
            --name peruse-ci \
            --read-only \
            --cap-drop=ALL \
            --security-opt=no-new-privileges \
            -p 127.0.0.1:8080:8080 \
            --mount type=bind,src="${GITHUB_WORKSPACE}",dst=/data,readonly \
            peruse:ci
          trap 'docker rm -f peruse-ci >/dev/null 2>&1 || true' EXIT
          curl --fail --retry 10 --retry-all-errors --retry-delay 1 http://127.0.0.1:8080/__peruse/health
          curl --fail --retry 10 --retry-all-errors --retry-delay 1 http://127.0.0.1:8080/ | grep -q 'Directory contents'
          curl --fail http://127.0.0.1:8080/ | grep -q 'class="repo-toolbar"'
